# Docker Compose Files for Mkdocs Material

These `compose` files are separated into three separate files:

## > Build your site
`docker-compose-build.yaml`

## > Serve your site
`docker-compose-serve.yaml`

## > Deploy your site
`docker-compose-deploy.yaml`
